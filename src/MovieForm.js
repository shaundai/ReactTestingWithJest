import React from 'react'

const MovieForm = () => {
        return (
                <form data-testid="movie-form">
                    <input type="text" />
                    <button>
                        Submit
                    </button>
                </form>
        )
}

export default MovieForm